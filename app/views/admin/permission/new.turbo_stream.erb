<%= turbo_stream.replace "remote_modal" do %>
  <%= render partial: 'admin/permission/form_modal', locals: { url: @url, url_method: @url_method, title: "Crear Permiso", view: @view, permission: @permission } %>
<% end %>
